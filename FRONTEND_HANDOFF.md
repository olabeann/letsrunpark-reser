# 렛츠런파크 통합 예약 프론트엔드

## 실행

`index.html`을 더블클릭하면 별도 서버나 설치 없이 서울 예약 화면이 바로 실행됩니다.

## 전달 파일

- `index.html`: 포니 타기·포니랑 놀기 상품 목록, 상품 옵션, 장바구니, 주문·결제 화면
- `pony.html`: 서울 포니 승마체험 상세
- `tour.html`: 서울 렛츠런파크 투어 상세
- `tokens.css`: 기존 LETSRUN PLAY 디자인 시스템 원본 토큰
- `components.css`: 디자인 시스템의 Button, Chip, Badge, Card 컴포넌트
- `styles.css`: 화면 컴포넌트 및 모바일 반응형 스타일
- `app.js`: 화면 전환, 예약 선택, 검색, 회차 차단 등 프로토타입 상호작용
- `booking-rules.js`: 이용 시간 구간 비교, 계정 기준 중복 검사, 장바구니 검증·가격 계산 및 일괄 예약 생성

## 구현 범위

상품 목록은 `index.html`, 개별 상품은 `index.html?product=ride` 또는 `index.html?product=play`, 장바구니는 `index.html?view=cart`로 연결합니다. 기존 `?program=...` 링크는 상품 목록으로 연결됩니다. 투어 상품은 새 예약 목록에서 제외하고, 기존 예약 기록은 유지합니다. 예약 서비스 탑바는 LetsrunPlay와 분리된 독립 브랜드로 구성했습니다.

이 결과물은 개발 연동 전 단계의 서울 전용 프론트엔드 프로토타입입니다. 결제, 로그인, 예약 저장은 실제 API 대신 화면 동작으로 구현되어 있습니다.

상품 목록 → 상품 상세에서 날짜·시간·인원 선택 → 담기 완료 안내 → 별도 장바구니 → 주문·결제 → 회차별 묶음 티켓 흐름을 제공합니다. 상품은 포니 타기·포니랑 놀기 두 가지입니다. 중복 검사는 예약자 계정의 기존 예약 및 장바구니 전체를 대상으로 하며, 부분적으로라도 이용 구간이 겹치면 차단합니다. 이전 투어 예약도 기존 80분 구간으로 검사하며 종료 시각에 이어 시작하는 회차는 허용합니다. 실제 서버에서는 인증된 회원 ID와 트랜잭션·PG 검증을 통해 동일 정책을 다시 강제해야 합니다.

997px에서 태블릿 레이아웃, 640px에서 모바일 상품·장바구니 레이아웃으로 전환합니다. 기존 디자인 토큰과 티켓 화면은 유지합니다.
